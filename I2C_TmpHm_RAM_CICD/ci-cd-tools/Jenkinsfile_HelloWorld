pipeline {
    agent any

    environment {
        TOOL_DIR = "${WORKSPACE}\\ci-cd-tools"
    }

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World.'
            }
        }
        stage('Build') { 
            steps {
                echo 'Explore Build Stage.'
                bat "\"${TOOL_DIR}\\buildi.bat\" Debug all"
            }
        }
    }
}